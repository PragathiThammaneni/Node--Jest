<!-- Steps:

1. Install Node JS which will also have npm 
2. Install Visual Studio
3. Go to Terminal and run > npm init >>> to install the node module and will create package.json
4. run > npm install >>> creates dependency node modules and will create node_module folder and package-lock.json
5. run > npm intall --save-dev jest 
6. Go to package.json and change the following 
    "scripts": {
    "test": "jest"
  }
->